```python
def P(n, score):  
    if (n == 1) and (score + 1 >= 31 or score + 10 >= 31):  
        return 0  
    if (n == 3) and score >= 31:  
        return True, False  
    if (n == 3) and (score + 1 >= 31 or score+10 >= 31):  
        return 0  
    if (n == 3) and (False, True) in I(n+1, score + 1) and (False, True) in I(n+1, score+10):  
        return False, True  
    if (n == 5) and score >= 31:  
        return False, True  
    if n > 5:  
        return 0  
    return [I(n+1, score+1), I(n+1, score+10)]  
  
def I(n, score):  
    return [P(n+1, score+1), P(n+1, score+10)]  
  
  
def  handler(obj, flag):  
    if isinstance(obj, list):  
        obj1, obj2 = handler(obj[0], not flag), handler(obj[1], not  flag)  
        if obj1 == obj2:  
            return obj1  
        if 0 in [obj1, obj2] and ((False, True) in [obj1, obj2] or (True, False) in [obj1, obj2]) and flag:  
            return (False, True) if (False, True) in [obj1, obj2] else (True, False)  
        if 0 in [obj1, obj2] and not flag:  
            return 0  
        if (False, True) in [obj1, obj2] and (True, False) in [obj1, obj2]:  
            return True, False  
    return obj  
  
for S in range(1, 30):  
    arr = P(1, S)  
    if type(arr) == list:  
        print(S)  
        print(handler(arr[0], True))  
        print(handler(arr[1], True))  
        print("-----------")  
    else:  
        pass
```

Данное решение состоит из простого создания график в результате пинг-понга, а потом его рекурсивной обработки и ручному отсеву