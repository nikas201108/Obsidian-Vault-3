#Python 

`Product` это функция библиотеки `itertools`, которая позволяет производить декартовое произведение (иными словами, комбинировать элементы):

```python
from itertools import product  
  
print(list(product("ОР", "ОР"))) #Орёл и решка: [('О', 'О'), ('О', 'Р'), ('Р', 'О'), ('Р', 'Р')]
```